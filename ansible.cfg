[defaults]


# vagrant

# hostfile = vagrant_hosts
# remote_user = vagrant
# private_key_file = .vagrant/machines/default/virtualbox/private_key
# host_key_checking = False
# roles_path = ./roles

# docker
allow_world_readable_tmpfiles = True
hostfile = docker_hosts
remote_user = docker
host_key_checking = False